# 对话决策与进度时间线

> 记录关键决策链，避免新会话重新讨论已定事项。

## 2026-06 决策链

### 阶段 1：需求采访（Q1–Q6）
逐题确认：业务运营+网关聚合 → 国内为主 → 语言不限 → 混合通道(C) → 大规模 → 纯开源。

### 阶段 2：首次尝试 qfjiaoyan/sms-platform
- clone 成功，但**无 SQL 脚本**，无法初始化数据库
- 放弃

### 阶段 3：尝试 liuyanning/sms_platform（信通畅邮）
- clone 成功，有完整 SQL + 文档 + 在线演示
- 创建库 `sms_business` / `sms_send`
- ❌ 导入 business.sql 报 `Row size too large`（ROW_FORMAT=COMPACT 与 MariaDB 10.11 不兼容）
- 当时未解决，暂停

### 阶段 4：转 playSMS（PHP，国际项目）
- clone + install-playsms.sh + 手动补数据库导入 + 配置
- 踩坑：PHP7.4 不兼容 playSMS1.4.8（`int|null` 联合类型）→ 切 PHP8.2 解决
- ✅ 部署成功，跑在 **8877 端口**（admin/admin123）
- 但发现：**不支持国内协议**，国内中间商用不了

### 阶段 5：网络排查
- Web 打不开 → 诊断：阿里云安全组只放行了 SSH 65022
- 改 Apache 端口 80→8080(被freeswitch占)→**8877**
- 用户在阿里云安全组放行 8877 后访问成功

### 阶段 6：用户决定换项目
- playSMS 不行（不支持国内协议）
- 重新搜索：Gitee + 搜索引擎 + GitHub
- 发现 austin（6067 stars，活跃，docker）但角色相反（发送方非平台）
- 用户澄清：**"我是中间商，上游甲方HTTP，下游运营商"**

### 阶段 7：最终决策
- austin 排除（角色相反）
- **回到信通畅邮**（liuyanning/sms_platform）—— 唯一完整匹配中间商需求
- SQL 兼容问题已定方案（sed 改 ROW_FORMAT）
- 通道模式确认为 C（混合）
- **待执行**：在 `/tools/sms` 重新部署信通畅邮

### 阶段 8：信通畅邮部署成功（2026-06-29）

按 docs/03 部署，实际结构与原假设多处出入，逐一踩坑修复，最终部署成功。

**部署结果**：
- 3 平台公网可访问：admin(8889) / enterprise(8890) / netway(8888)
- 运营平台 http://36.139.116.238:8889/public/admin/login.jsp（admin/123456）
- DB：sms_business(41表) / sms_send(11表)，专用用户 `sms/smsGateway2026`
- 3 Tomcat 实例 0 错误，Druid 双数据源连接成功

**关键路径**：clone → 建库 → sed 修 SQL(ROW_FORMAT) → 导入 → 改 datasource.properties → mvn package → 3 WAR 部署到 3 Tomcat ROOT → 启动

**踩坑与解决**：
1. 配置文件叫 `datasource.properties`（非 README 写的 database.properties，README 自己写错）
2. 工程嵌套结构：`sms-system/sms-admin/sms-admin-web`（非平级三工程）
3. sms-quick 1.0.15 私有依赖（Maven 中央仓库无）→ gitee clone quick-sms + mvn install，主 pom 版本 1.0.9→1.0.15
4. **Java 17 不兼容**（javax.xml.bind/JAX-WS/sun.misc.BASE64Encoder 移除）→ 切 Temurin Java 8（Debian 12 无 openjdk-8，清华镜像下 binary 到 /tools/jdk8）
5. Debian 12 无 tomcat9（只有 tomcat10，jakarta 命名空间不兼容 javax）→ 清华镜像下 Tomcat 9.0.119 binary 到 /tools/tomcat9
6. MariaDB root 仅 socket 免密，JDBC 走 TCP 3306 被拒(ERROR 1698) → 建专用 `sms` 用户
7. **business.sql 首次导入只建 24/41 表**（旧库残留致 admin_limit id=1 duplicate，mysql 后续中止）→ DROP + `mysql --force` 干净重导，41 表全建
8. Tomcat 多实例：3 份 tomcat9 副本(/tools/tms-netway|admin|enterprise)，各改 server.xml 三端口 + setenv.sh 指向 Java 8

### 阶段 9：品牌定制 + 台账/日志定制方案（2026-06-30）

- **品牌定制**：平台公司名/品牌词（庄点科技/信相通/drondea）改"信速应"。两层：code 表 UPDATE（platform_name/admin_home_information/签名/告警模板，38行）+ 源码 jsp sed（defaultValue/公告/接口文档）。改 code 表后重启刷新 DatabaseCache。**关键发现：品牌配置真源是 `code` 表（非 properties 空表）。**
- **合规需求评估**：对照《台账/日志管理法则》，评估信通畅邮覆盖度——日志88%、业务台账75%、合作方50%、号码30%、期限0%。
- **定制开发方案**：产出 `docs/05-台账日志定制方案.md`（5 需求 R1-R5、表设计、4 批次、约8-10天、6 个待确认项）。待审批后分批实施。

### 阶段 10：本地 git 仓库 + 第1批 R2 操作员角色（2026-06-30）

**方案审批通过**（第六节 6 待确认项拍板）：
- 开发方式：本地 git 开发 → deploy-sms.sh 部署服务器
- 操作员权限：能发短信+查记录+看自己财务，禁改通道/审核
- 签名审核：审核员审 + 未审禁止发送
- 留存时限：日志12月/台账36月/凭证60月（全部留更长，注意磁盘扩容）
- 字段加密：暂不做（监管强制再评估）
- 测试数据：开发前清理（先备份）

**本地 git 仓库建立**：
- 仓库根 `sms-gateway/`，分支 main；首次提交文档 `2eedcf4`
- rsync 拉服务器源码到 `src/sms_platform/`（排除 target/.git/mydata/docs，28M/1791 文件），基线提交 `ea3f72b`
- `.gitignore` 排除编译产物/IDE/logs；datasource.properties 含 DB 密码（仓库勿公开推送）

**第1批 R2 操作员角色（零代码数据配置，已验证 ✅）**：
- 备份 5 张权限表 → `/tools/sms/backup/role_perm_baseline_20260630.sql`
- 新增角色 role_id=23 操作员(Operator)；权限白名单 65 项（006 短信除审核 + 007 报表三项 + 008003 充值记录 + 001005 密码 + 4 顶层菜单）
- 测试用户 user_id=82 `operator_test`（密码 MD5('123456')，复制 admin 哈希）
- **验证全通过**：登录成功 / 菜单恰好 4 项白名单 / 通道页"无权限!" / 发件箱正常 / **无需重启 DB 实时生效**

**关键发现**：
- RBAC 四表：`admin_role`(角色) + `admin_limit`(权限项/菜单 424 项) + `admin_role_limit`(角色↔权限) + `admin_user_roles`(用户↔角色多对多)；鉴权 `AdminLimitInterceptor` 实时查库
- `admin_user` 仅 admin 1 行有效（user_roles 有 40+ 孤儿映射，用户已删）；密码 MD5 无盐
- 权限数据运行时实时查 DB，**改权限无需重启**

**遗留**：孤儿映射/测试角色(12,13,16-22) 未清理（保守不删）；"看自己财务"的数据范围过滤属后续业务逻辑层，第1批仅菜单级。

## 当前状态

| 项 | 状态 |
|----|------|
| 需求确认 | ✅ 完成 |
| 选型决策 | ✅ 信通畅邮 |
| playSMS 部署 | ✅ 跑通但弃用 |
| 信通畅邮部署 | ✅ 完成（2026-06-29，见阶段 8） |
| 本地项目目录 | ✅ 已建（sms-gateway/） |
| 项目文档 | ✅ 已固化 |
| 本地 git 仓库 | ✅ 已建（main，文档+源码基线 ea3f72b） |
| 台账定制·第1批 R2 操作员角色 | ✅ 完成（2026-06-30，已验证） |

## 下一步行动

**台账定制开发（4 批次，约8-10天）**：
1. ✅ 第1批 R2 操作员角色（已完成）
2. ⏭ 第2批 R1 签名管理（建表 + Controller/Service/DAO + JSP 列表/新增/审核/启停 + 菜单 + 发送校验，1.5天）
3. 第3批 R3 管理员设置 + 时限清理定时任务 + 不可篡改(DB权限收紧)，2天
4. 第4批 R5 台账(扩字段+新表+预警+视图) + R4-A 拒收标记，4-6天

**日常运维（并行）**：
- 配上游通道（CMPP/HTTP）打通实际发送链路
- 创建企业客户测试客户端平台(8890)发送入口
- playSMS(8877) 确认弃用后按 docs/03 清理

## 部署运维速查

- 停/启 3 实例：`/tools/tms-{netway,admin,enterprise}/bin/{shutdown,startup}.sh`（用 Java 8，setenv.sh 已配）
- 重启脚本：`bash /tools/restart-sms.sh` / 部署脚本 `/tools/deploy-sms.sh`
- 日志：`/tools/tms-<name>/logs/catalina.out`
- 源码：`/tools/sms/sms_platform`（构建后 WAR 在各模块 target/）
- DB：`mysql -u sms -psmsGateway2026 sms_business`（TCP）/ `mysql -u root`（socket）
